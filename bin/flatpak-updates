#!/bin/bash
if test -f /usr/bin/flatpak; then
  updates_count=$(flatpak remote-ls --updates | wc -l)
  if [ "$updates_count" = '0' ]; then
    exit 0
  else
    notify-send --app-name="Flatpack Updates" "$updates_count"
    exit 0
  fi
fi
