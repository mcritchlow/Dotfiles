#!/bin/bash
if test -f /usr/bin/flatpak; then
  updates_count=$(flatpak remote-ls --updates | awk -v ORS=',' '{ print $1 }')
  if [ -z "$updates_count" ]; then
    exit 0
  else
    notify-send --app-name="Flatpack Updates" "$updates_count"
    exit 0
  fi
fi
